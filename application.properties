# App name
spring.application.name=taskmanager

# MySQL connection
spring.datasource.url=jdbc:mysql://localhost:3306/taskmanager_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=rootShravani

# JPA settings
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT settings (we'll use these in Stage 4)
jwt.secret=secureTaskManagementSystemSecretKey2024AbcXyz!!
jwt.expiration=3600000
jwt.refresh-expiration=604800000